# JIT Episodic Memory — Pinned Requirements
# Python 3.12  |  CUDA 12.x  |  Tested on RTX 4060 Ti 8 GB
#
# Install: pip install -r requirements.txt
# For full env (recommended): conda env create -f environment.yml
#
# IMPORTANT: transformers ≥5.0 renamed post_process_object_detection →
# post_process_grounded_object_detection. The code handles both APIs via
# a backward-compatible getattr chain (see retrieval/level3_verification.py).

# ── Core ML ──
torch==2.10.0
torchvision==0.25.0

# ── Vision-Language Models ──
open-clip-torch==3.2.0
transformers==5.1.0
timm==1.0.24

# ── Vector Search ──
faiss-cpu==1.13.2

# ── Clustering ──
scikit-learn==1.8.0

# ── Data Processing ──
numpy==2.4.2
pandas==3.0.0
pyarrow==23.0.0

# ── Visualization ──
matplotlib==3.10.8
pillow==12.1.1

# ── Utilities ──
tqdm==4.67.3
pyyaml==6.0.3
ftfy==6.3.1

# ── Habitat-Sim (not on PyPI) ──
# Option A: conda install habitat-sim -c conda-forge -c aihabitat
# Option B: build from source — see habitat/habitat-sim/BUILD_FROM_SOURCE.md
